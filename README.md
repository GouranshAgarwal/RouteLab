An Interactive Networking Playground
where users build custom networks (graphs) and visually simulate routing algorithms step-by-step, including routing table updates and packet flow explanations.

For now everything is working in the local env, now i need to make APIs for communicating the results 