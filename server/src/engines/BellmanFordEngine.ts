import { AlgorithmEngine } from "./AlgorithmEngine";

export class BellmanFordAlgorithmEngine implements AlgorithmEngine {
  run(input) {
    throw new Error("Bellman-Ford not implemented yet");
  }
}
